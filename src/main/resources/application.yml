#server:
#  port:3000

#spring:
#  main:
#    web-application-type: none

## or in properties: spring.main.web-application-type = none

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/customer
    username: postgres
    password: Irena123
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  main:
    web-application-type: servlet